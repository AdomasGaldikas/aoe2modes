# Template Mode

Scaffolding for a new mode. Copy the folder or run:

```
aoe2modes new my_mode --name "My Mode"
```

Then edit `mode.toml` (settings) and `build.py` (logic), and build with:

```
aoe2modes build my_mode
```
